import React from 'react';
import { AppContainer } from './AppContainer.ui';
import { UserStories } from './UserStories';

export const Application = () => (
  <AppContainer>
    <UserStories />
  </AppContainer>
);
